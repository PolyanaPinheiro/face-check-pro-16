import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'checklist.setup',
  appName: 'Projeto_CheckList_Setup_Qualidade',
  webDir: 'dist'
};

export default config;
